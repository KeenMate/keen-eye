export const mainFrameFilters = {
	urls: ["<all_urls>"],
	types: ["main_frame"],
};

export const fetchFilters = {
	urls: ["<all_urls>"],
	types: ["xmlhttprequest"],
};

export const extra = {
	requestHeaders: ["requestHeaders"],
	responseHeaders: ["responseHeaders"],
};
